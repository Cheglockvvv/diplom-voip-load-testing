[default]
exten => 1000,1,Answer()
 same => n,Playback(demo-congrats)
 same => n,Hangup()

[lab]
exten => 1000,1,Answer()
 same => n,Wait(1)
 same => n,Playback(demo-congrats)
 same => n,Hangup()

exten => 1001,1,Answer()
 same => n,Echo()
 same => n,Hangup()
