[default]
exten => 1000,1,Answer()
 same => n,Playback(demo-congrats)
 same => n,Hangup()

[lab]
exten => 1000,1,Dial(PJSIP/1000,20)
 same => n,Hangup()

exten => 1001,1,Dial(PJSIP/1001,20)
 same => n,Hangup()
